import styled from 'styled-components';

export const Divider = styled.hr`
  position: absolute;
  bottom: 0;

  width: 100%;
  height: 1px;

  border: none;

  background: linear-gradient(270deg, #323238, rgba(50, 50, 56, 0.25));
`;
